import styled from 'styled-components';

export const CoffeeListContainer = styled.main`
  max-width: calc(70rem + 1rem);
  margin: 0 auto;
  padding: 32px 1rem;
`;

export const CoffeeListHeaderContainer = styled.header`
  display: flex;
  align-items: center;
  justify-content: space-between;
`;
